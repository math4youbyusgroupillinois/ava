from django.db import models

from apps.ava_core.models import TimeStampedModel

#class LDAPEdges(TimeStampedModel)
#    name = models.CharField(max_length=50)
#    source = models.ForeignKey('apps.ava_core_ldap.ActiveDirectoryUser')
#    target = models.ForeignKey('apps.ava_core_ldap.ActiveDirectoryGroup')

 #   def __unicode__(self):
 #       return self.name or u''
